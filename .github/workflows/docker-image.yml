name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build MariaDB Image
      run: docker build mariadb --file mariadb/Dockerfile --tag mariadb:$(date +%y.%m.%d)

    - name: Build Nginx Image
      run: docker build nginx --file nginx/Dockerfile --tag nginx:$(date +%y.%m.%d)

    - name: Build WordPress Image
      run: docker build wordpress --file wordpress/Dockerfile --tag wordpress:$(date +%y.%m.%d)

    - name: Push MariaDB Image
      run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
    - run: docker tag mariadb:$(date +%y.%m.%d) ${{ secrets.DOCKER_USERNAME }}/mariadb:$(date +%y.%m.%d)
    - run: docker push ${{ secrets.DOCKER_USERNAME }}/mariadb:$(date +%y.%m.%d)

    - name: Push Nginx Image
      run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
    - run: docker tag nginx:$(date +%y.%m.%d) ${{ secrets.DOCKER_USERNAME }}/nginx:$(date +%y.%m.%d)
    - run: docker push ${{ secrets.DOCKER_USERNAME }}/nginx:$(date +%y.%m.%d)

    - name: Push WordPress Image
      run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
    - run: docker tag wordpress:$(date +%y.%m.%d) ${{ secrets.DOCKER_USERNAME }}/wordpress:$(date +%y.%m.%d)
    - run: docker push ${{ secrets.DOCKER_USERNAME }}/wordpress:$(date +%y.%m.%d)

